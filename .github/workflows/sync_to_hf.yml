name: Sync to Hugging Face Hub
on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  sync-to-hub:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          lfs: true
      - name: Push to hub
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          git push --force https://xenoroses:$HF_TOKEN@huggingface.co/spaces/xenoroses/Nym-Bot main || git push --force https://xenoroses:$HF_TOKEN@huggingface.co/spaces/xenoroses/nym-bot main || git push --force https://xenoroses:$HF_TOKEN@huggingface.co/spaces/xenoroses/Project-Nym main
